# creating a small FasApi app called TaskTracker
# Below endpoints are covered:
#   POST   /tasks         — create a task
#   GET    /tasks         — list all tasks (NEW for you: list endpoint!)
#   GET    /tasks/{id}    — get one task
#   PUT    /tasks/{id}    — update a task
#   DELETE /tasks/{id}    — delete a task